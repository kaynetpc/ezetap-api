/* eslint-disable no-unused-vars */
export interface UserEntity {
  password: string;
  email: string;
  firstName: string;
  lastName: string;
  address: string;
  phoneNumber: string;
  token: string;
  refreshToken: string;
};
